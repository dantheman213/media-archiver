# Custom NSIS script for Media Archiver
# This template can be used to customize the installer.

!macro CUSTOM_INSTALL_CONF
    # Custom installation configuration
!macroend

!macro CUSTOM_UNINSTALL_CONF
    # Custom uninstallation configuration
!macroend

!macro CUSTOM_INSTALL_FILES
    # Custom files to install
!macroend

!macro CUSTOM_UNINSTALL_FILES
    # Custom files to uninstall
!macroend
